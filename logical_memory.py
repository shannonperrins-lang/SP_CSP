# SP, Logical and physical memory notes

name = "Alex"

age = 14

score = 10

score =25

height = 5.9

city = "Denver"

zip_code = 80202

price1 = 10

price2 = 10

print(f"the vriable name has a value of {name} and is saved at {id(name)}")

print(f"the vriable age has a value of {age} and is saved at {id(age)}")

print(f"the vriable score value of {score} and is saved at {id(score)}")

print(f"the vriable as score value of {score} and is saved at {id(score)}")

print(f"the vriable as height value of {height} and is saved at {id(height)}")

print(f"the vriable as city value of {city} and is saved at {id(city)}")

print(f"the vriable as zip_code value of { zip_code} and is saved at {id(zip_code)}")

print(f"the vriable as price1 value of {price1} and is saved at {id(price1)}")

print(f"the vriable as price2  value of {price2} and is saved at {id(price2)}")